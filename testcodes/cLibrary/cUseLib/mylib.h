#ifndef MYLIB_H
#define MYLIB_H

// py-ctypes에 관한 내용은 아님. 필요없음.
// /usr/include 혹은 /usr/local/include 에 들어갈 내용

#include <stddef.h>
int arraySum(int* arr, size_t size);
void printWelcome(char* name);


#endif
