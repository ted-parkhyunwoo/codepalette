// js는 let과 const가 꼭 대치되는형태로 사용됨. 다른언어의 const int 등과는 좀 다름.
const PI = 3.14;

// windows.prompt 는 문자열로 저장할 수 있음. console과 마찬가지로 windows. 생략가능.
const user_input = prompt("dialog box message: ");
console.log(user_input);