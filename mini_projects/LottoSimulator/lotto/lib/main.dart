import 'package:flutter/material.dart';
import 'package:lotto/widgets/main_screen.dart';

// main_screen (stateful widget) -> welcome_screen(stateless) 로 띄운 후 logit_screen 으로 변경

void main() {
  runApp(const MainScreen());
}
